/**
 * Studio CAD UI
 *
 * Barra compatta a comandi diretti e icone SVG originali.  Non modifica
 * disegni, livelli o entita; agisce solo sull'interfaccia della sessione.
 */
include("StudioCadUI.js");

function init(basePath) {
    StudioCadUI.init(basePath);
}
